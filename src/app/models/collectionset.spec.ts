/* tslint:disable:no-unused-variable */

import {
  beforeEach, beforeEachProviders,
  describe, xdescribe,
  expect, it, xit,
  async, inject
} from '@angular/core/testing';
import {CollectionSet} from './collectionset';

describe('Set', () => {
  it('should create an instance', () => {
    expect(new CollectionSet()).toBeTruthy();
  });
});
